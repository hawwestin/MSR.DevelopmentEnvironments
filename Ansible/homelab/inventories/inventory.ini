# Newer add one IP address to more than two groups. 
# As variable squashing are merged in ASCII order.
# Even If variable is in different group that is not executed
# ansible WILL use last loaded value for that host :/
# https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html#how-variables-are-merged  

# New host provisioning, add new host to [new] group to create ansible user and ssh key for that host.
[new] 


[new:vars]
ansible_user=root
;ansible_password= ; use -k to ask for password
;ansible_become_pass= ; use -K to ask for password if not root.

[testing]


[testing:vars]
ansible_user=ansible_mgr
# remote_user=ansible
# ansible_become_user=root
# ansible_ssh_private_key_file= ~/.ssh/id_rsa
# ansible_become_method = su
; ansible_python_interpreter= /usr/bin/python3

[portainer]
192.168.50.10 
[plex]
192.168.50.11
[NginxProxyManager]
192.168.50.14
[PiholeLxc]
192.168.50.15
[cockpit]
192.168.50.21
[fileserver]
192.168.50.22


[homeassistant]
192.168.100.14

[homeassistant:vars]
auto_install_module_deps=true

[heimdall]
192.168.100.15

##################
; Danger ZONE! 
[malina]
rpi_ups ansible_host=192.168.50.20 
[malina:vars]
ansible_user=ansible_mgr
ansible_ssh_private_key_file=~/.ssh/id_ansible

[proxmox] 
192.168.50.5
192.168.50.6

[proxmox:vars]
ansible_user=notset
ansible_ssh_private_key_file=~/.ssh/id_rsa

[qnap]
192.168.50.247

[qnap:vars]
ansible_user=notset
ansible_ssh_private_key_file=~/.ssh/id_rsa



[lxc:children]
homeassistant
heimdall
portainer
plex
NginxProxyManager
PiholeLxc
cockpit
fileserver


[lxc:vars]
ansible_user=ansible_mgr
ansible_become_user=root 
ansible_ssh_private_key_file=~/.ssh/id_ansible
ansible_become_method = su