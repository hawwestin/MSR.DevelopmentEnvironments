---
- hosts: web
  become: yes
  tasks:

  - name: Sync WordPress files
    synchronize:
      src: "../files/backup/html/"
      dest: "/var/www/html/"
      recursive: yes
      delete: no

  - name: Fix permissions
    file:
      path: /var/www/html
      owner: www-data
      group: www-data
      recurse: yes

- hosts: db
  become: yes
  tasks:

  - name: Import SQL dump
    mysql_db:
      name: wordpress
      state: import
      target: "../files/backup/dump.sql"
      login_user: root
      login_password: "rootpass"
